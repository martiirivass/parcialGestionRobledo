# Specification: Rate Limiting on Authentication

Delta spec for `rate-limiting-auth` capability introduced in US-001-auth.

This capability is fully covered under `user-authentication` spec in the User Login requirement, specifically the "Rate limiting on login" scenario.
