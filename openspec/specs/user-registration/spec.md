# Specification: User Registration

Delta spec for `user-registration` capability introduced in US-001-auth.

This capability is fully covered under `user-authentication` spec in the core registration and validation flows.
